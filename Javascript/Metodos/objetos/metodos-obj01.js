const produto = {
    nome: "Notebook",
    preco: 2500,
    disponibilidade: true
};

console.log(Object.keys(produto)); // Exibe as chaves do objeto
console.log(Object.values(produto)); // Exibe os valores do objeto
console.log(Object.entries(produto)); // Exibe as entradas (chave-valor) do objeto